import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  loadingProgress: {
    position: "absolute",
    top: 400,
    left: 400,
    zIndex: 1
  },
  productGrid:{
    padding: "10px",
  }
}));